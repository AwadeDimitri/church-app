query GetPosts($limit: Int = 20, $offset: Int = 0) {
  posts(limit: $limit, offset: $offset, order_by: { created_at: desc }) {
    id
    title
    content
    category
    cover_url
    published_at
    created_at
  }
}
